# TOMO Market Backend - Environment Variables Template
# Copy this file to .env and fill in your values

# Environment
NODE_ENV=production

# Server
PORT=5000

# JWT Secret (CHANGE THIS TO A STRONG RANDOM STRING!)
JWT_SECRET=your-super-secret-jwt-key-change-this-to-something-very-strong-and-random

# Database
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://tomouser:YOUR_PASSWORD_HERE@localhost:5432/tomomarket

# Domain (production — final)
# Customer site: https://tomo-sa.com
# Backend API:   https://api.tomo-sa.com
DOMAIN=tomo-sa.com

# Optional: allow create-test-accounts endpoint (dev only; disabled in production unless set)
# ALLOW_TEST_ACCOUNTS=true

# Fallback login has been REMOVED. Authentication is API-only (JWT + DB).
# JWT_SECRET is required in production (min 32 chars). No hardcoded credentials.

# إنشاء Super Admin من سطر الأوامر (مرة واحدة، آمن):
#   cd backend
#   set CREATE_SUPER_ADMIN_EMAIL=admin@tomo.com
#   set CREATE_SUPER_ADMIN_NAME=مدير النظام
#   set CREATE_SUPER_ADMIN_PASSWORD=كلمة_مرور_قوية_8_أحرف
#   node server.js --create-super-admin
# (Linux/Mac: export CREATE_SUPER_ADMIN_*=... ثم node server.js --create-super-admin)
# CREATE_SUPER_ADMIN_EMAIL=
# CREATE_SUPER_ADMIN_NAME=
# CREATE_SUPER_ADMIN_PASSWORD=

# بذرة مديرين (مرة واحدة): إنشاء super-admin@tomo.com و admin@tomo.com
#   set TOMO_ADMIN_PASSWORD=Tomo.2439
#   node seed-admin-users.js
# TOMO_ADMIN_PASSWORD=

# Optional: Email Configuration (if you add email features later)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# OAuth (customer signup) — optional; if missing, OAuth endpoints return 501
# Production callback URLs (fixed):
#   Google: https://api.tomo-sa.com/api/auth/oauth/google/callback
#   Apple:  https://api.tomo-sa.com/api/auth/oauth/apple/callback
# Google OAuth 2.0
# GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
# GOOGLE_OAUTH_REDIRECT_URL=https://api.tomo-sa.com/api/auth/oauth/google/callback
# Apple Sign In
# APPLE_CLIENT_ID=your-service-id (Service ID from Apple Developer)
# APPLE_TEAM_ID=your-team-id
# APPLE_KEY_ID=your-key-id
# APPLE_PRIVATE_KEY_P8=base64-or-path-to-.p8-file
# APPLE_REDIRECT_URL=https://api.tomo-sa.com/api/auth/oauth/apple/callback

