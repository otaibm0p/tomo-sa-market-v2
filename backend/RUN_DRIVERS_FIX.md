# 🚀 تشغيل إصلاح جدول Drivers

## الطريقة 1: استخدام Node.js (موصى به)

```bash
cd backend
node run-drivers-fix-simple.js
```

## الطريقة 2: استخدام psql مباشرة

إذا كان `psql` مثبتًا ومتاحًا في PATH:

```bash
cd backend
psql -U tomo_admin -d tomo_db -f migrations/0007_fix_drivers_foreign_key.sql
```

## الطريقة 3: استخدام migrate.js

```bash
cd backend
node migrate.js
```

(سيقوم بتشغيل جميع المايجريشنات غير المطبقة)

---

## ⚠️ متطلبات

1. **PostgreSQL يجب أن يكون قيد التشغيل**
   ```bash
   # Windows (إذا كان مثبتًا كخدمة)
   # يجب أن تكون الخدمة قيد التشغيل
   
   # للتحقق
   # افتح Services (services.msc) وابحث عن "postgresql"
   ```

2. **بيانات الاتصال صحيحة**
   - الافتراضي: `postgresql://tomo_admin:StrongPass123@localhost:5432/tomo_db`
   - أو من ملف `.env`: `DATABASE_URL`

---

## 🔧 استكشاف الأخطاء

### خطأ: `ECONNREFUSED`
**المشكلة:** PostgreSQL غير قيد التشغيل أو غير متاح

**الحل:**
1. تأكد أن PostgreSQL قيد التشغيل
2. تحقق من المنفذ (5432)
3. تحقق من بيانات الاتصال

### خطأ: `password authentication failed`
**المشكلة:** كلمة المرور أو اسم المستخدم خاطئ

**الحل:**
1. تحقق من ملف `.env`
2. أو عدّل `run-drivers-fix-simple.js` ببيانات الاتصال الصحيحة

### خطأ: `database does not exist`
**المشكلة:** قاعدة البيانات غير موجودة

**الحل:**
```sql
CREATE DATABASE tomo_db;
```

---

## ✅ التحقق من النتيجة

بعد التشغيل الناجح، يجب أن ترى:

```
✅ PRIMARY KEY: PRIMARY KEY (user_id)
✅ FOREIGN KEY: FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
```

---

**ملاحظة:** إذا كان PostgreSQL غير متاح محليًا، يمكنك تشغيل المايجريشن على السيرفر بعد رفع الملفات.
