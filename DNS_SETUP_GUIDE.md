# 🌐 دليل إعداد DNS - tomo-sa.com

## 📋 الوضع الحالي

من الصورة التي أرسلتها، أرى أن DNS Records موجودة على **DigitalOcean**:
- A Record: `tomo-sa.com` → `138.68.245.29` ✅
- CNAME: `www.tomo-sa.com` → `ns3.digitalocean.com`
- NS Records: `ns1.digitalocean.com`, `ns2.digitalocean.com`, `ns3.digitalocean.com`

---

## ⚠️ المشكلة المحتملة

إذا كان الدومين `tomo-sa.com` مسجل على **Hostinger**، فهناك خياران:

### الخيار 1: استخدام DigitalOcean Nameservers (مُوصى به) ✅

إذا كنت تريد استخدام DNS في DigitalOcean (كما هو موضح في الصورة):

1. **اذهب إلى Hostinger hPanel**
2. **اختر دومين `tomo-sa.com`**
3. **اذهب إلى "DNS / Name Servers"**
4. **غيّر Nameservers إلى:**
   ```
   ns1.digitalocean.com
   ns2.digitalocean.com
   ns3.digitalocean.com
   ```
5. **احفظ التغييرات**
6. **انتظر 5-30 دقيقة** حتى يتم نشر DNS

### الخيار 2: استخدام Hostinger DNS مباشرة

إذا كنت تريد استخدام DNS في Hostinger بدلاً من DigitalOcean:

1. **في Hostinger hPanel:**
   - اذهب إلى DNS
   - أضف A Record:
     - **Type:** A
     - **Name:** `@` (أو اتركه فارغاً)
     - **Value:** `138.68.245.29`
     - **TTL:** `3600`
   - أضف A Record:
     - **Type:** A
     - **Name:** `www`
     - **Value:** `138.68.245.29`
     - **TTL:** `3600`

2. **في DigitalOcean:**
   - احذف DNS Records الموجودة (اختياري)
   - أو اتركها كما هي (لن تؤثر)

---

## ✅ التغييرات في الكود

التغييرات التي تمت في `backend/server.js`:

1. **CORS Settings:** تم إضافة `tomo-sa.com` إلى allowed origins ✅
2. **Dynamic Droplet IP:** يمكن إضافة IP من متغير البيئة

**هذه التغييرات ستعمل تلقائياً بعد:**
- رفع الملفات الجديدة إلى الخادم
- إعادة تشغيل PM2: `pm2 restart tomo-market-backend`

---

## 🔧 الخطوات التالية

### إذا كنت تستخدم DigitalOcean DNS (كما في الصورة):

1. **تأكد من Nameservers في Hostinger:**
   - يجب أن تكون: `ns1.digitalocean.com`, `ns2.digitalocean.com`, `ns3.digitalocean.com`

2. **تحقق من A Record في DigitalOcean:**
   - يجب أن يكون: `tomo-sa.com` → `138.68.245.29`
   - يجب أن يكون: `www.tomo-sa.com` → `138.68.245.29` (A Record، ليس CNAME)

3. **أصلح CNAME في DigitalOcean:**
   - احذف CNAME: `www.tomo-sa.com` → `ns3.digitalocean.com`
   - أضف A Record: `www.tomo-sa.com` → `138.68.245.29`

### إذا كنت تستخدم Hostinger DNS:

1. **في Hostinger:**
   - أضف A Records كما هو موضح أعلاه

2. **في DigitalOcean:**
   - يمكنك حذف DNS Records (اختياري)

---

## 🧪 اختبار DNS

بعد إعداد DNS، اختبر:

```bash
# من جهازك المحلي
nslookup tomo-sa.com
nslookup www.tomo-sa.com

# يجب أن يعيد IP: 138.68.245.29
```

أو استخدم موقع:
- https://dnschecker.org
- أدخل `tomo-sa.com` وتحقق من أن A Record يعيد `138.68.245.29`

---

## 📝 ملخص

1. **التغييرات في الكود:** ✅ جاهزة، ستعمل بعد رفع الملفات وإعادة تشغيل PM2
2. **DNS:** 
   - إذا Nameservers في Hostinger تشير إلى DigitalOcean → كل شيء جاهز ✅
   - إذا لا → غيّر Nameservers أو استخدم Hostinger DNS مباشرة
3. **أصلح CNAME:** غيّر `www.tomo-sa.com` من CNAME إلى A Record

---

## ❓ أسئلة شائعة

**س: هل يجب أن أغير شيئاً في DigitalOcean؟**
ج: نعم، غيّر `www.tomo-sa.com` من CNAME إلى A Record يشير إلى `138.68.245.29`

**س: هل التغييرات في الكود ستعمل تلقائياً؟**
ج: نعم، بعد رفع الملفات الجديدة وإعادة تشغيل PM2

**س: كم يستغرق نشر DNS؟**
ج: عادة 5-30 دقيقة، لكن قد يصل إلى 48 ساعة في بعض الحالات

---

**🎯 الخطوة التالية:** أصلح CNAME في DigitalOcean إلى A Record، ثم اختبر الموقع!

